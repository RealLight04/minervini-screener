# Python
__pycache__/
*.pyc
venv/
.venv/

# 환경변수 (비밀값)
.env

# OpenDartReader가 만드는 corp_codes 캐시 (재생성 가능)
docs_cache/

# 주의: screener.db 는 배포용 스냅샷이라 .gitignore 하지 않고 커밋에 포함함
backtest_results.csv
.omc/
alerts.db
# 서버·재시작 로그 (재생성 가능)
*.log
# 연구용 생성 데이터 (재생성 가능, 대용량) — 스크립트만 커밋
scripts/research/data/
# DB 로컬 백업 사본 (대용량). 배포·이력용 스냅샷은 screener.db 커밋으로 관리
data/backups/
